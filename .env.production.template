# Configuración para TheCookFlow en Producción (VPS)
# Copia este archivo como .env en tu VPS y completa los valores

# Entorno
NODE_ENV=production
PORT=3000

# Base de datos PostgreSQL
# Reemplaza con los datos de tu PostgreSQL en el VPS
DATABASE_URL=postgresql://thecookflow_user:TU_PASSWORD_SEGURO@localhost:5432/thecookflow

# Variables de PostgreSQL individuales
PGHOST=localhost
PGPORT=5432
PGUSER=thecookflow_user
PGPASSWORD=TU_PASSWORD_SEGURO
PGDATABASE=thecookflow

# OpenAI API
OPENAI_API_KEY=sk-proj-TU_OPENAI_API_KEY_AQUI

# Stripe (ELIMINADO - Ya no se usan pagos)
# STRIPE_SECRET_KEY=sk_live_TU_STRIPE_SECRET_KEY_PRODUCCION
# VITE_STRIPE_PUBLIC_KEY=pk_live_TU_STRIPE_PUBLIC_KEY_PRODUCCION
# La aplicación ahora es completamente gratuita

# Autenticación y Sesiones
SESSION_SECRET=un_secreto_muy_largo_y_seguro_para_sesiones_min_32_caracteres
REPLIT_DOMAINS=thecookflow.com,www.thecookflow.com
REPL_ID=782d6408-67e0-41bf-bfdb-eee0c66bf523
ISSUER_URL=https://replit.com/oidc

# Configuración adicional que puede ser necesaria
# Google Cloud Storage (si usas subida de imágenes)
# GOOGLE_CLOUD_PROJECT_ID=tu-proyecto-gcp
# GOOGLE_CLOUD_KEY_FILE=/path/to/service-account-key.json

# URLs y dominios
FRONTEND_URL=https://thecookflow.com
BACKEND_URL=https://thecookflow.com

# Configuración de correo (si implementas notificaciones)
# SMTP_HOST=tu-servidor-smtp.com
# SMTP_PORT=587
# SMTP_USER=tu-email@dominio.com
# SMTP_PASS=tu-password-email

# Configuración de logs
LOG_LEVEL=info

# Seguridad adicional
CORS_ORIGIN=https://thecookflow.com
COOKIE_SECURE=true
COOKIE_SAME_SITE=strict